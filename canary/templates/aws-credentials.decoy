# CONFIRMED-SECRET-OK: decoy honeytoken. The values below are FAKE placeholders
# (literally contain PLACEHOLDER / EXAMPLE). They are bait, not live credentials.
#
# DECOY FILE — DO NOT USE. Planted by the ClickFix Defense Kit canary generator.
# Marker: @@MARKER@@
# Created: @@CREATED@@
#
# This is a honeytoken. If you are reading this and you are NOT the machine owner,
# these credentials are a tripwire: using them alerts the owner that you breached
# this machine. Replace @@AWS_KEY@@ with a REAL key minted at canarytokens.org to
# make `aws sts get-caller-identity` phone home with the source IP.
[default]
aws_access_key_id = @@AWS_KEY@@
aws_secret_access_key = @@AWS_SECRET@@
region = us-east-1
